# NX CrossCompare 개발 계획 (v2)

> 이 문서는 `개발계획_초안.md`를 기반으로 논의를 거쳐 구체화된 버전입니다.
> 초안은 원본 컨셉 기록용으로 남겨두고, 실제 작업은 이 문서를 기준으로 진행합니다.
> 회사PC 또는 다른 담당자가 이어받을 경우, "왜 이렇게 결정했는지"를 §10, §11, §16, 변경이력에서 확인하세요.

---

## 1. 프로젝트 개요

TV 기구설계의 횡전개(43/50/55/65/75/85인치 등, 최대 8~10개 동시 비교 가능해야 함) NX/JT 모델을
한 화면에서 비교하고, 사용자가 정의한 측정 포인트를 모든 인치에서 자동 추적하여 치수를
비교하는 플랫폼.

AI의 역할은 "치수를 판단"하는 것이 아니라:
- 규칙 추천
- 포인트 후보 추천
- 개발 보조

---

## 2. 개발 원칙

- C++ : Geometry Engine, JT 처리, Viewer, Core Logic
- Python : Excel, 분석, 리포트, AI 보조
- SQLite : 프로젝트/규칙/포인트 저장
- UI와 Core를 완전 분리
- 모듈 단위 개발
- Git 기반 관리

---

## 3. 목표 기능

1. 프로젝트 생성
2. JT/NX 파일 등록
3. 여러 인치 동시 표시 (최대 8~10개)
4. 화면 회전/확대/이동 동기화
5. 사용자가 규칙 기반 측정포인트 생성
6. 동일 포인트 자동 탐색
7. 치수 계산
8. 하단 비교 테이블 표시
9. Excel Export
10. 프로젝트 저장/불러오기

---

## 4. 파일 포맷 결정 — JT (.jt)

**결론: JT 포맷 채택, facet(LOD)/exact B-rep 하이브리드 로딩 전략**

이유:
- Siemens NX/Teamcenter 생태계 표준 경량 시각화 포맷 → 회사 PLM 프로세스와 호환성 높음
- **내장 LOD(Level of Detail) + 스트리밍 구조** — 8~10개 인치 동시 로드 시 성능 부담을 줄이는 핵심 요소
- Facet(테셀레이션)과 Exact B-rep(Parasolid/XT 임베드)을 함께 담을 수 있어, 뷰어는 가벼운 facet으로
  운용하고 실제 치수 계산이 필요한 순간에만 정밀 B-rep에 접근하는 하이브리드 구성 가능
- JT Open Toolkit(무료 SDK)으로 C++에서 직접 파싱 가능 → 회사PC용 실제 Adapter 구현과 부합

대안 비교:

| 포맷 | 장점 | 단점 |
|---|---|---|
| **JT** (채택) | LOD/스트리밍, exact B-rep 겸용 | 파싱 SDK 학습 필요 |
| STEP(.stp) | 정밀 B-rep 표준, 범용 | LOD 없음 → 다중 로드 시 느림/무거움 |
| Parasolid(.x_t) | 정밀, NX 커널 그대로 | 무거움, SDK 라이선스 비용 |
| STL/glTF | 가벼움 | 근사 메시라 ±0.05mm 수준 공차 측정엔 부적합 |

**확인 필요(회사PC 단계에서)**: 회사 PLM에서 뽑는 JT가 facet-only인지 exact B-rep까지
임베드된 버전인지 — Phase4b(face_to_face_gap) 정밀도에 직접 영향.

---

## 5. 좌표계 설계 원칙 — 절대좌표 중심정렬

**전제(회사 확인 사항)**: 입고되는 도면은 동일 아이템의 인치별 데이터이며, 어셈블리 중심이
**절대좌표 원점(0,0,0)에 정렬**되어 들어오는 것이 회사 컨벤션이다.

**설계에 미치는 영향**:
- 중심이 같다고 포인트 값 자체가 같아지는 것은 아님 (43인치 후크와 85인치 후크는 원점으로부터
  거리가 다름 — 그게 비교하려는 대상). 절대좌표 정렬이 주는 이득은 **좌표계(원점+축방향)가
  공통이라 모델별 정렬(registration) 단계 없이 바로 같은 좌표 이웃에서 대응 포인트를 탐색할 수
  있다는 점**.
- Reference Frame 우선순위에 `World_Origin`을 1순위로 추가. 기존 `BLU_Center`, `Datum_CSYS` 등은
  이 컨벤션이 깨지는 예외 케이스(로컬 데이텀 기준 feature)를 위한 fallback으로 유지.
- **Phase2(JT Loader)에 정렬 QC 검증 로직 필요**: 파일 입고 시마다
  - bounding box center 또는 지정 Datum이 원점 근처인지 자동 확인 (임계값 이내 아니면 경고)
  - 축 방향(X/Y/Z 컨벤션)이 인치 간 동일한지 확인 (축이 다르면 절대좌표 비교 전제 자체가 깨짐)
- 규칙 스키마의 `reference_frame` 필드는 기존 구조 유지, 기본값만 `World_Origin` 우선으로 조정.

---

## 6. 측정 타입 (Measurement Types)

| type | 설명 | 예시 |
|---|---|---|
| point_to_point | 3D 두 점 사이 거리 | Boss-Screw 정렬 |
| point_to_plane | 기준 평면에서 점까지 수직거리(법선 방향) | 후크 높이 |
| axis_projection | 특정 축(X/Y/Z) 방향으로 투영한 거리 | 전장길이, 후크 걸림량 |
| face_to_face_gap | 두 면 사이 최단거리(형상 테셀레이션 기반) | Rib-OpenCell Gap, 살두께 |

```
Measurement {
  type: point_to_point | point_to_plane | axis_projection | face_to_face_gap
  targets: [ref_id_1, ref_id_2]      // Point 또는 Plane/Axis id
  projection: "3D" | "X" | "Y" | "Z" | "normal"
  tolerance_plus_mm: float
  tolerance_minus_mm: float          // 비대칭 공차 지원 (예: 후크 걸림량 +0.3/-0.1)
}
```

주의: face_to_face_gap은 Geometry Engine(C++)에 면 간 최단거리 탐색 알고리즘이 필요해
포인트 기반 매칭보다 구현 난이도가 높다. Phase4는 point 기반을 먼저 구현하고,
face_to_face_gap은 Phase4b로 분리한다.

---

## 7. 규칙 스키마 설계 (Rule Schema)

이 툴의 핵심은 "치수를 판단"하는 것이 아니라 **설계자가 포인트를 선택하는 기준을
구조화해서 재사용**하는 것이다. 규칙은 3개 레이어로 분리한다.

1. **Anchor** — 기준이 되는 지오메트리 프리미티브 (Hole, Face, Vertex, Edge Mid 등 + 파라미터)
2. **Reference Frame** — 상대좌표 계산 기준. **1순위 `World_Origin`**, fallback으로
   `BLU_Center`, `Open_Cell`, `Datum_CSYS` 등 (§5 참고)
3. **Selector** — 여러 후보 중 하나를 고르는 정렬/필터 기준

```
Rule {
  anchor_type: string          // Hole, Face, Vertex, Edge_Mid, Boss_Center, Hook_Tip_Edge ...
  anchor_param: json           // {diameter: 3.2, tolerance: 0.05} 등 형상별 파라미터
  reference_frame: [string]    // 우선순위 리스트, 예: [World_Origin, BLU_Center, Datum_CSYS]
  selector: [string]           // 아래 selector enum 참고, 배열로 다중 조건 지정 가능
  measurement_type: string     // §6 측정 타입 중 하나
  tolerance_plus_mm: float
  tolerance_minus_mm: float
  extra_params: json           // 향후 확장 여유 필드
}
```

**Selector enum (현재까지 확인된 패턴)**
- `nearest_pair` — 두 부품 간 가장 가까운 요소 쌍
- `nearest_to_point` — 특정 참조 포인트에 가장 가까움
- `leftmost` / `rightmost` — 방향 기준 첫 번째
- `nearest_face_pair` — 가장 가까운 면 쌍
- `parallel_face_pair` — 법선이 반대방향인 가장 가까운 면 쌍 (살두께 등)

### 규칙 예시 카탈로그 (사출 조립구조)

**① Boss-Screw 체결 정렬**
```
anchor_A: Hole(Ø2.8, part=Bezel) / anchor_B: Boss_Center(Ø2.6, part=Rear_Chassis)
reference_frame: [World_Origin, Datum_CSYS] / selector: nearest_pair
measurement: point_to_point, projection=3D, tol=±0.15
```

**② 후크 높이**
```
anchor: Hook_Tip_Edge(part=Side_Frame) / reference_plane: Datum_Plane(Rear_Chassis_Base)
selector: leftmost
measurement: point_to_plane, projection=normal, tol=±0.1
```

**③ 후크 걸림량 (전장길이류)**
```
anchor_A: Hook_Catch_Edge(Side_Frame) / anchor_B: Wall_Inner_Edge(Front_Bezel)
reference_frame: [World_Origin]
measurement: axis_projection, projection=Z, tol=+0.3/-0.1  // 비대칭 공차
```

**④ Rib-OpenCell Gap**
```
anchor_A: Face(Rib_Top_Surface) / anchor_B: Face(OpenCell_Edge)
selector: nearest_face_pair
measurement: face_to_face_gap, tol=±0.2
```

**⑤ 살두께 (Boss Root Wall Thickness)**
```
anchor_A: Face(Boss_Outer_Wall) / anchor_B: Face(Boss_Inner_Wall)
selector: parallel_face_pair
measurement: face_to_face_gap, projection=normal, tol=±0.05
```

> 이 5개가 규칙의 전부가 아님. `docs/rule_catalog.md`를 살아있는 문서로 운용하며
> 회사PC에서 실제 모델을 다루면서 계속 추가한다 (§13 Phase4 참고).

### 규칙 라이브러리 (1.5단계)

Phase3~4 직후, AI 추천 없이 단순 템플릿 복사만으로 가치가 있는 회사 표준 규칙 세트.
예: LED↔Screw, Cell↔Bezel, Rib↔Open Cell, PCB↔Boss, Clip↔Side Frame.
신규 프로젝트에서는 라이브러리 불러오기만으로 규칙 세팅 완료.

### 향후 AI 확장 (2단계)

- 신규 포인트 정의 시 기존 규칙과의 유사도(anchor_type 일치 + reference_frame 겹침 +
  selector 패턴 일치 기반 벡터 유사도) 계산 → "OO 규칙과 97% 유사, 적용할까요?" 추천
- 규칙 선택 패턴(자주 쓰는 Hole/Face 기준, 자주 비교하는 치수 조합) 누적 학습 →
  신규 프로젝트에서 후보 자동 제안, Confidence 표시, 클릭 승인
- **전제조건**: Phase6까지 쌓인 실사용 규칙 데이터(카탈로그)가 충분해야 의미 있음

---

## 8. 검증 전략 — 회사PC 이전 사전검증 (합성 데이터 기반)

**배경**: 실제 JT/NX 파일은 보안상 회사 밖으로 반출 불가. 회사PC 검증 전, 개인PC에서
매칭 로직(Reference Frame/Selector/Tolerance 판정)이 논리적으로 타당한지 확인할 방법이 필요.

**검토했던 대안과 기각 사유**:
- 인터넷의 일반 사출 설계 가이드 이미지에서 좌표를 눈대중으로 추출하는 방법을 검토했으나 기각.
  - 이미지 검색 + 비전 분석 + 좌표 추정 반복 → 토큰 비용 큼
  - 더 결정적으로, 이미지에서 눈으로 읽은 좌표는 근사치라 "정답(ground truth)" 자체가
    부정확해짐 → 검증 기준이 애매해짐

**채택한 방법: 좌표 직접 정의 + 규칙 기반 합성 데이터 생성**

1. 좌표를 이미지 소싱 없이 코드/JSON으로 직접 정의 (보스 중심, 후크 팁, 홀 중심 등 anchor 포인트)
2. 인치별 변형 생성 시 **단순 균일 스케일이 아니라 실무 패턴을 모사**:
   - 전체 폭/높이, 포인트 간 배치 간격 → 인치 비율대로 스케일
   - 보스 지름, 후크 높이, 살두께 등 → 인치와 무관하게 고정값 유지
   - 중심은 항상 절대좌표 원점 (§5 전제와 일치)
3. 각 포인트에 "인치 불변" / "인치 비례" 라벨을 붙여 Ground Truth로 저장
   → 이것이 tolerance 판정 로직의 정답값 역할
4. Mock Adapter가 이 합성 좌표셋을 반환하도록 구성
5. 자동 유닛테스트로 검증: Selector가 6개 인치에서 동일 anchor를 정확히 찾는가,
   계산된 값이 fixed 항목은 거의 동일하게, scaled 항목은 예상 비율대로 나오는가

**이 방법의 한계**: face_to_face_gap 같은 3D 정밀도(테셀레이션 기반 최단거리)까지는
검증하지 못함 — 이 부분은 회사PC 통합테스트에서 별도 확인 필요 (Phase4b).

**장점 요약**: 토큰 효율적(이미지 소싱/비전분석 불필요), 정답값이 정확함(직접 정의했으므로),
실무 패턴(고정값 vs 스케일값 혼재)을 반영해 알고리즘의 핵심 가치를 제대로 검증 가능.

---

## 9. 개발 환경 분리 전략 (개인PC / 회사PC)

Core(Rule Engine, SQLite, Excel Export, UI 로직)는 실제 JT 파일 없이도 개발·테스트
가능해야 한다. NX/JT Adapter는 인터페이스(`IGeometryAdapter`)로 분리하고
개인PC/회사PC용 구현체를 교체 가능하게 한다.

- **개인PC**: Core 로직 + Mock Adapter(§8의 합성 데이터)로 개발/단위테스트
- **회사PC**: 실제 JT Open Toolkit/NX 연동 Adapter로 교체 후 통합 테스트만 수행
  (실제 JT/NX 파일 및 관련 이미지는 회사 밖으로 반출하지 않음 — 보안 요건)

**워크플로우**
1. 개인PC: feature 브랜치에서 Core + Mock Adapter로 개발/단위테스트
2. Git push → 회사PC에서 pull
3. 회사PC: 실제 Adapter로 교체 후 통합 테스트만 수행
4. 결과/버그는 이슈로 남겨 개인PC에서 수정 → 반복

---

## 10. UI 구조

좌측: 프로젝트 / 규칙 관리 / 화면 설정 / 가져오기·내보내기 / 옵션
중앙: 다중 Viewer(최대 8~10개) / 인치 선택 / 동기 회전 / 동기 확대 / Overlay(향후)
우측: 포인트 그룹 / 포인트 검색 / 규칙 관리
하단: 치수 비교 테이블 / 복사 / Excel Export

---

## 11. 아키텍처

```
Presentation(UI)
↓
Application(Service)
↓
Rule Engine
↓
Geometry Engine(C++)
↓
NX/JT Adapter (IGeometryAdapter — Mock/실제 교체 가능)
↓
SQLite
↓
Python Analysis
```

---

## 12. 폴더 구조

```
src/
 ui/
 core/
 geometry/
 rule/
 viewer/
 database/
 export/
 python/
 tests/
 docs/
   rule_catalog.md      // 규칙 카탈로그 (지속 업데이트)
```

---

## 13. 단계별 개발 계획

### Phase 0. 준비
- Git 저장소 초기화, 폴더 구조 생성
- `docs/rule_catalog.md` 생성 (§7의 5개 예시 규칙으로 시작)
- CMake 기본 세팅

산출물: 빈 저장소 + 폴더 구조 + 규칙 카탈로그 뼈대

### Phase 1. 기본 골격
- UI 셸(레이아웃만), Core-UI 경계 확정
- `IGeometryAdapter` 인터페이스 정의 + Mock Adapter 스텁

산출물: 실행 가능한 빈 프로그램

### Phase 2. Viewer + 정렬 QC
- Mock Adapter로 다중 인치 더미 지오메트리 표시, 카메라 동기화
- **[신규] JT Loader에 정렬 QC 검증 로직**: 어셈블리 중심이 원점 근처인지,
  축 방향이 인치 간 동일한지 자동 확인 (§5)

산출물: Mock 데이터로 다중 인치 동시 비교 뷰어 동작 + 정렬 검증 로직

### Phase 3. 규칙 스키마 인프라 + 합성 데이터 검증
- SQLite 스키마: Project / Rule(Anchor+ReferenceFrame+Selector+Measurement) / Point
- `reference_frame` 1순위 `World_Origin` 반영 (§5)
- **[신규] §8 합성 데이터 생성 파이프라인 구축** — 좌표 직접 정의, 인치별
  고정값/스케일값 규칙 적용, Ground Truth 라벨링
- 규칙 1종(Boss-Screw, point_to_point)으로 저장/불러오기 + 매칭 로직 왕복 테스트

산출물: 프로젝트+규칙 저장/불러오기 가능, 합성 데이터 기반 매칭 로직 자동검증 통과

### Phase 4. Point 기반 자동 탐색 + 치수 계산
- Reference Frame 기반 매칭 (World_Origin 우선), Selector 로직 구현
  (`nearest_pair`, `nearest_to_point`, `leftmost`/`rightmost`)
- 비교 테이블 UI
- **회사PC 전환 시점**: 실제 JT 모델에 규칙 하나씩 적용, `rule_catalog.md`에
  실사용 규칙 계속 추가

산출물: 실제(회사) 모델 기준 point 계열 규칙 3~4종 자동 비교 성공

### Phase 4b. Face 기반 측정 (별도 분리)
- 면 테셀레이션 기반 최단거리 탐색 (C++ Geometry Engine)
- `nearest_face_pair`, `parallel_face_pair` selector
- 회사 JT 데이터가 exact B-rep 임베드인지 여부 확인 필요 (§4)

산출물: Rib-OpenCell Gap, 살두께 자동 비교

### Phase 5. Export/리포트
- Excel Export(Python), 화면 캡처, 보고서 생성

### Phase 6. 안정화
- 예외처리, 성능 최적화, 테스트 보강
- `rule_catalog.md`를 "1.5단계 규칙 라이브러리"로 정리/승격

### Phase 7. AI 확장
- 규칙 유사도 기반 추천, 자동 문서화
- 전제조건: Phase6까지의 실사용 규칙 데이터 축적

---

## 14. Claude Code 작업 규칙

- 한 번에 하나의 모듈만 구현
- 구현 전 설계 설명 → 클래스 다이어그램 제안 → 코드 작성 → 단위 테스트 작성 → 리팩토링 → README 업데이트
- 각 모듈 종료 시 변경점 / 의존성 / 다음 단계를 항상 정리

---

## 15. 변경 이력 (초안 대비)

| 항목 | 초안 | v2 |
|---|---|---|
| 파일 포맷 | JT 언급만 있고 근거 없음 | JT 채택 근거 명시, facet/exact B-rep 하이브리드 전략 (§4) |
| 좌표계 | Reference Frame만으로 상대좌표 매칭 | 입고 도면이 절대좌표 중심정렬됨을 전제로 반영, `World_Origin` 1순위 추가, Phase2에 QC 검증 추가 (§5) |
| 검증 전략 | 없음 | 회사PC 이전 합성 데이터 기반 사전검증 전략 신규 수립, 이미지 기반 방법은 검토 후 기각 (§8) |
| Phase2 | Viewer만 | 정렬 QC 검증 로직 추가 |
| Phase3 | 규칙 저장만 | 합성 데이터 생성 파이프라인 구축 추가 |
| 규칙 생성 UI | 없음 (코드에 하드코딩) | 폼 기반 1차 버전 신설, Face/Point 피킹은 2차 버전으로 분리 (§16) |
| 기본 측정 항목 | 없음 | 전장 사이즈(X/Y/Z)를 anchor 탐색 없는 기본 세팅 항목으로 추가 (§7) |

---

## 16. 규칙 생성 UI (1차 버전 — 폼 기반, 피킹은 2차 버전)

### 배경

사용자는 코딩을 할 수 없으므로, 신규 규칙을 추가하려면 "A면/B면을 선택하고 Gap을 고르면
그 사이 거리를 측정" 같은 3D 피킹 UI가 궁극적으로 필요하다. 하지만 Face 피킹은 실제
면 테셀레이션 데이터(레이캐스팅 + NX Open API로 "이 면이 뭔지" 역질의)가 있어야 의미가
있어서, 회사PC의 실제 어댑터(Face 데이터)가 갖춰지기 전까지는 검증할 방법이 없다.

**결정**: 1차 버전은 피킹 없이 "폼 입력"으로 규칙을 만들고, 2차 버전(회사PC, 실제 Face
데이터 확보 후)에서 "클릭하면 이 폼이 자동으로 채워지는" 방식으로 확장한다. 즉 1차
버전의 폼이 2차 버전의 자동 입력 대상이 되므로 헛수고가 아니다.

### 1차 버전 폼 구성 (구현됨 — `src/ui/RuleEditorDialog`)

```
[측정 타입]     ▼ point_to_point / point_to_plane / axis_projection / face_to_face_gap
[Anchor A]      형상타입 ▼ (Hole/Boss_Center/Face 등)   부품명 ▼ (Bezel/Rib 등)
[Anchor B]      형상타입 ▼                                부품명 ▼
[Selector]      ▼ nearest_pair / leftmost / rightmost / nearest_face_pair / parallel_face_pair
[공차]          + ___ mm   - ___ mm
[저장] → 모든 인치에 자동 적용
```

- `point_to_plane` 선택 시 Anchor B 대신 "기준평면 타입/부품명" 입력으로 전환 (§7 PlaneRef)
- 저장 시 `reference_frame`은 항상 `[World_Origin]`으로 고정 (§5 기본값, UI 노출은 추후 필요시)
- 인치 하나에서 anchor_type 기반으로 규칙을 만들면, RuleEngine이 나머지 인치에도 같은
  anchor_type으로 자동 탐색·적용한다 (Rule이 좌표가 아니라 anchor_type을 저장하기 때문에
  별도 처리 없이 이미 이렇게 동작함 — §7)

### 2차 버전 확장 방향 (회사PC, 미착수)

1. 3D 뷰포트에서 면/점 클릭 → 레이캐스팅으로 대상 식별
2. NX Open API로 "이 형상이 뭔지" 역질의 (원통형 면 지름, 평면 위치, 부품명 등)
3. 위 폼 필드를 자동으로 채움 → 사용자는 확인/미세조정만
4. Rule/RuleEngine/DB 스키마는 1차 버전 그대로 재사용 (변경 불필요)
