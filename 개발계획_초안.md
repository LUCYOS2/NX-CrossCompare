# Claude Code 개발 프롬프트 (한국어 상세 버전)

# 프로젝트명
NX CrossCompare (가칭)

## 프로젝트 컨셉
TV 기구설계의 횡전개(43/50/55/65/75/85인치) NX/JT 모델을 한 화면에서 비교하고,
사용자가 정의한 측정 포인트를 모든 인치에서 자동 추적하여 치수를 비교하는 플랫폼을 개발한다.

AI의 역할은 '치수를 판단'하는 것이 아니라
- 규칙 추천
- 포인트 후보 추천
- 개발 보조
이다.

---

# 개발 원칙

- C++ : Geometry Engine, JT 처리, Viewer, Core Logic
- Python : Excel, 분석, 리포트, AI 보조
- SQLite : 프로젝트/규칙/포인트 저장
- UI와 Core를 완전 분리
- 모듈 단위 개발
- Git 기반 관리

---

# 목표 기능

1. 프로젝트 생성
2. JT/NX 파일 등록
3. 여러 인치 동시 표시
4. 화면 회전/확대/이동 동기화
5. 사용자가 규칙 기반 측정포인트 생성
6. 동일 포인트 자동 탐색
7. 치수 계산
8. 하단 비교 테이블 표시
9. Excel Export
10. 프로젝트 저장/불러오기

---

# 측정 타입 (Measurement Types)

포인트 간 거리 외에도 실무에서 확인하는 치수는 성격이 다양하다.
"포인트 대 포인트"만으로는 후크 높이, Gap, 살두께를 표현할 수 없으므로
Reference Geometry(Plane/Axis/CSYS)를 Point와 별도 엔티티로 관리하고,
측정 타입을 4종으로 분류한다.

| type | 설명 | 예시 |
|---|---|---|
| point_to_point | 3D 두 점 사이 거리 | Boss-Screw 정렬 |
| point_to_plane | 기준 평면에서 점까지 수직거리(법선 방향) | 후크 높이 |
| axis_projection | 특정 축(X/Y/Z) 방향으로 투영한 거리 | 전장길이, 후크 걸림량 |
| face_to_face_gap | 두 면 사이 최단거리(형상 테셀레이션 기반) | Rib-OpenCell Gap, 살두께 |

```
Measurement {
  type: point_to_point | point_to_plane | axis_projection | face_to_face_gap
  targets: [ref_id_1, ref_id_2]      // Point 또는 Plane/Axis id
  projection: "3D" | "X" | "Y" | "Z" | "normal"
  tolerance_plus_mm: float
  tolerance_minus_mm: float          // 비대칭 공차 지원 (예: 후크 걸림량 +0.3/-0.1)
}
```

주의: face_to_face_gap은 Geometry Engine(C++)에 면 간 최단거리 탐색 알고리즘이 필요해
포인트 기반 매칭보다 구현 난이도가 높다. Phase4는 point 기반을 먼저 구현하고,
face_to_face_gap은 Phase4 후반 또는 별도 Phase로 분리한다.

---

# 규칙 스키마 설계 (Rule Schema)

이 툴의 핵심은 "치수를 판단"하는 것이 아니라 **설계자가 포인트를 선택하는 기준을
구조화해서 재사용**하는 것이다. 규칙은 3개 레이어로 분리한다.

1. **Anchor** — 기준이 되는 지오메트리 프리미티브 (Hole, Face, Vertex, Edge Mid 등 + 파라미터)
2. **Reference Frame** — 상대좌표 계산 기준 (BLU Center, Open Cell, Datum CSYS 등). 절대좌표가 아닌
   이 기준 상대좌표로 저장해야 인치가 달라도(스케일이 달라도) 동일 포인트로 매칭 가능
3. **Selector** — 여러 후보 중 하나를 고르는 정렬/필터 기준 (가장 가까운, 좌측 첫 번째 등)

```
Rule {
  anchor_type: string          // Hole, Face, Vertex, Edge_Mid, Boss_Center, Hook_Tip_Edge ...
  anchor_param: json           // {diameter: 3.2, tolerance: 0.05} 등 형상별 파라미터
  reference_frame: [string]    // 우선순위 리스트, 예: [BLU_Center, Open_Cell, Datum_CSYS]
  selector: [string]           // 아래 selector enum 참고, 배열로 다중 조건 지정 가능
  measurement_type: string     // 위 Measurement Types 중 하나
  tolerance_plus_mm: float
  tolerance_minus_mm: float
  extra_params: json           // 향후 확장 여유 필드
}
```

**Selector enum (현재까지 확인된 패턴)**
- `nearest_pair` — 두 부품 간 가장 가까운 요소 쌍
- `nearest_to_point` — 특정 참조 포인트에 가장 가까움
- `leftmost` / `rightmost` — 방향 기준 첫 번째
- `nearest_face_pair` — 가장 가까운 면 쌍
- `parallel_face_pair` — 법선이 반대방향인 가장 가까운 면 쌍 (살두께 등)

## 규칙 예시 카탈로그 (사출 조립구조)

**① Boss-Screw 체결 정렬**
```
anchor_A: Hole(Ø2.8, part=Bezel) / anchor_B: Boss_Center(Ø2.6, part=Rear_Chassis)
reference_frame: [BLU_Center, Datum_CSYS] / selector: nearest_pair
measurement: point_to_point, projection=3D, tol=±0.15
```

**② 후크 높이**
```
anchor: Hook_Tip_Edge(part=Side_Frame) / reference_plane: Datum_Plane(Rear_Chassis_Base)
selector: leftmost
measurement: point_to_plane, projection=normal, tol=±0.1
```

**③ 후크 걸림량 (전장길이류)**
```
anchor_A: Hook_Catch_Edge(Side_Frame) / anchor_B: Wall_Inner_Edge(Front_Bezel)
reference_frame: [Datum_CSYS]
measurement: axis_projection, projection=Z, tol=+0.3/-0.1  // 비대칭 공차
```

**④ Rib-OpenCell Gap**
```
anchor_A: Face(Rib_Top_Surface) / anchor_B: Face(OpenCell_Edge)
selector: nearest_face_pair
measurement: face_to_face_gap, tol=±0.2
```

**⑤ 살두께 (Boss Root Wall Thickness)**
```
anchor_A: Face(Boss_Outer_Wall) / anchor_B: Face(Boss_Inner_Wall)
selector: parallel_face_pair
measurement: face_to_face_gap, projection=normal, tol=±0.05
```

## 규칙 라이브러리 (1.5단계)

Phase3~4 직후, AI 추천 없이 단순 템플릿 복사만으로 가치가 있는 회사 표준 규칙 세트.
예: LED↔Screw, Cell↔Bezel, Rib↔Open Cell, PCB↔Boss, Clip↔Side Frame.
신규 프로젝트에서는 라이브러리 불러오기만으로 규칙 세팅 완료.

## 향후 AI 확장 (2단계)

- 신규 포인트 정의 시 기존 규칙과의 유사도(anchor_type 일치 + reference_frame 겹침 +
  selector 패턴 일치 기반 벡터 유사도) 계산 → "OO 규칙과 97% 유사, 적용할까요?" 추천
- 규칙 선택 패턴(자주 쓰는 Hole/Face 기준, 자주 비교하는 치수 조합) 누적 학습 →
  신규 프로젝트에서 후보 자동 제안, Confidence 표시, 클릭 승인

---

# 개발 환경 분리 전략 (개인PC / 회사PC)

Core(Rule Engine, SQLite, Excel Export, UI 로직)는 실제 JT 파일 없이도 개발·테스트
가능해야 한다. NX/JT Adapter는 인터페이스(`IGeometryAdapter`)로 분리하고
개인PC/회사PC용 구현체를 교체 가능하게 한다.

- **개인PC**: Core 로직 + Mock Adapter(더미 지오메트리, 합성 테스트 데이터)로 개발/단위테스트
- **회사PC**: 실제 JT Open Toolkit/NX 연동 Adapter로 교체 후 통합 테스트만 수행
  (실제 JT/NX 파일은 회사 밖으로 반출하지 않음 — 보안 요건)

**워크플로우**
1. 개인PC: feature 브랜치에서 Core + Mock Adapter로 개발/단위테스트
2. Git push → 회사PC에서 pull
3. 회사PC: 실제 Adapter로 교체 후 통합 테스트만 수행
4. 결과/버그는 이슈로 남겨 개인PC에서 수정 → 반복

---

# UI 구조

좌측
- 프로젝트
- 규칙 관리
- 화면 설정
- 가져오기/내보내기
- 옵션

중앙
- 다중 Viewer
- 인치 선택
- 동기 회전
- 동기 확대
- Overlay(향후)

우측
- 포인트 그룹
- 포인트 검색
- 규칙 관리

하단
- 치수 비교 테이블
- 복사
- Excel Export

---

# 아키텍처

Presentation(UI)

↓

Application(Service)

↓

Rule Engine

↓

Geometry Engine(C++)

↓

NX/JT Adapter

↓

SQLite

↓

Python Analysis

---

# 폴더 구조

src/
 ui/
 core/
 geometry/
 rule/
 viewer/
 database/
 export/
 python/
 tests/
 docs/

---

# 단계별 개발 계획

## Phase1
- 프로젝트 생성
- 기본 UI
- Git 환경
- CMake

산출물
- 실행 가능한 빈 프로그램

## Phase2
- JT Loader
- Viewer
- 다중 모델 표시
- 카메라 동기화

산출물
- 여러 인치 동시 비교

## Phase3
- 규칙 생성
- 포인트 저장
- SQLite 연동

산출물
- 프로젝트 저장 가능

## Phase4
- 동일 포인트 탐색
- 치수 계산 (point_to_point, point_to_plane, axis_projection 우선 구현)
- 데이터 테이블

산출물
- 자동 비교 (point 기반)

## Phase4b (면 기반 측정, 별도 분리)
- face_to_face_gap 알고리즘 (면 테셀레이션 기반 최단거리 탐색)
- Rib-OpenCell Gap, 살두께 등

산출물
- Gap/살두께 자동 비교

## Phase5
- Excel Export
- 캡처
- 보고서

## Phase6
- 성능 최적화
- 예외처리
- 테스트

## Phase7(향후 AI)
- 규칙 추천
- 유사 포인트 추천
- 자동 문서 작성

---

# Claude Code 작업 규칙

- 한 번에 하나의 모듈만 구현
- 구현 전 설계 설명
- 클래스 다이어그램 제안
- 코드 작성
- 단위 테스트 작성
- 리팩토링
- README 업데이트

각 모듈 종료 시
- 변경점
- 의존성
- 다음 단계
를 항상 정리한다.

